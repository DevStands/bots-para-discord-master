# Bot Discord 

Bot desenvolvido para testes e automação.

## 🚀 Como Iniciar

Siga os passos abaixo para rodar o bot no seu computador:

1.  **Instale as dependências:**
    Abra o terminal na pasta do projeto e execute:

    ```bash
    npm install
    ```

2.  **Configure o ambiente:**

    - Renomeie ou copie o arquivo `.env.example` (se houver) ou crie um arquivo `.env`.
    - Adicione seu `DISCORD_TOKEN` e `CLIENT_ID`.

3.  **Ligue o bot:**
    Execute o comando:
    ```bash
    npm start
    ```

## 🛠️ Comandos Disponíveis

- `/painel`: Abre o painel administrativo.
- `/falar`: Faz o bot falar.
- `/embed`: Cria uma mensagem embed.

Testes
